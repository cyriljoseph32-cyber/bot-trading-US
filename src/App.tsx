// La page racine "/" affiche directement le Signal Bot (actifs US).
// L'ancienne landing "Coco" reste disponible dans l'historique Git.
import TradingApp from "./trading/TradingApp";

export default function App() {
  return <TradingApp />;
}
