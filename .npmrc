# Tolère les conflits de peer-dependencies lors de l'installation (Vercel + local).
# Nécessaire car Vitest et des versions récentes de Vite/React/TS déclarent des
# plages de peers strictes ; l'install échouerait sinon. N'affecte pas le build.
legacy-peer-deps=true
